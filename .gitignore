# ── 敏感憑證（絕對不能上傳）────────────────────────
credentials.json
token.json

# ── 設定檔（含密碼，改用 config.example.yaml 代替）─
config.yaml

# ── Python ───────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# ── 系統產生的暫存檔 ─────────────────────────────────
latest_report.html
*.log
